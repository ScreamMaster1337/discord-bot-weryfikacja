//const { REST, Routes } = require('discord.js');
//const config = require("../config.js");

//const clientId = config["Opcje"].id;
//const guildId = config["powitanie"].server;
//const token = config["Opcje"].token;

//const rest = new REST().setToken(token);

//rest.delete(Routes.applicationGuildCommand(clientId, guildId, '1103611206092206135'))
    //.then(() => console.log('Pomyślnie usunięto podaną komendę na serwerze!'))
    //.catch(console.error);